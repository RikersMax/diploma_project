<div class="content-form-container-create-session">
		
	<%= form_with(model: object_user, url: session_index_path, method: 'POST', data: { turbo: false }) do |f| %>


		<div class="text-bottom-create-object size-xxl">
			<p>Вход  в аккаунт</p>			
		</div>

	<%# f.fields_for :user do |u|  %>

		<div class="form-container-field-object">
			<%= f.label(:email, value: 'email', class: 'm-top-m size-xl')  %>
			<%= f.text_field(:email, palaceholder: 'email', class: 'form-input-create-object size-xl m-top-s', required: true) %>
		</div>

		<div class="form-container-field-object">
			<%= f.label(:password, value: 'password first', class: 'm-top-m size-xl')  %>
			<%= f.password_field(:password, class: 'form-input-create-object size-xl m-top-s', required: true)  %>
		</div>

	<%# end %>

		<div class="form-container-field-object m-bot-l m-top-m">			
			<%= f.submit(btn_sign_in_sign_up(controller)[:btn], class: 'btn-create-object')  %>
		</div>

	<% end %>
	
</div>